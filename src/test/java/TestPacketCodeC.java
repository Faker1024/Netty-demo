import org.junit.Test;

public class TestPacketCodeC {

    @Test
    public void testDecode(){
//        LoginRequestPacket request = LoginRequestPacket.builder().username("test").password("123").userId(UUID.randomUUID().toString()).build();
//        System.out.println(request.toString());
//        ByteBuf bytes = PacketCodeC.INSTANCE.encode(ctx.alloc(), request);
//        System.out.println(bytes.toString());
//        Packet result = PacketCodeC.INSTANCE.decode(bytes);
//        System.out.println(result.toString());
    }
}
